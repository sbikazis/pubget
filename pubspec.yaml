name: pubget
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.2+18

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.6.0
  provider: ^6.1.5+1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true