import 'package:flutter/material.dart';
import '../../edits/screens/edit_feed_page.dart';

/// Reels keeps the proven vertical playback implementation while the data
/// source is isolated in [FirebaseReelsRepository].
final class ReelsFeedPage extends StatelessWidget {
  const ReelsFeedPage({super.key});

  @override
  Widget build(BuildContext context) => const EditFeedPage();
}